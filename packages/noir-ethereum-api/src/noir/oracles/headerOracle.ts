import { ForeignCallOutput } from "@noir-lang/noir_js";
import { BlockHeader } from "../../ethereum/blockHeader.js";


export function serializeBlockHeader(blockHeader: BlockHeader): ForeignCallOutput[] {
    return [];
}
